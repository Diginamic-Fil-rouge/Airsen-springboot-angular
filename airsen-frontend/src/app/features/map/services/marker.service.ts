import { Injectable } from '@angular/core';

/**
 * MarkerService
 *
 * Purpose: Utilities to build AQI markers (DivIcons / SVG) with colors and
 * labels derived from commune air quality data.
 */
@Injectable({ providedIn: 'root' })
export class MarkerService {}

