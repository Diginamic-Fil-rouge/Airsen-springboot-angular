import { Injectable } from '@angular/core';

/**
 * CommuneDataService
 *
 * Purpose: Thin wrapper adapter around existing services (Geographic,
 * AirQuality, Weather) to expose a unified API for the new map.
 */
@Injectable({ providedIn: 'root' })
export class CommuneDataService {}

